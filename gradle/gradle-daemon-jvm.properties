# Pins the JVM the Gradle daemon runs on (Loom requires the daemon itself on this version).
# Gradle auto-detects installed JDKs (e.g. ~/.jdks, IDE-provisioned, setup-java on CI).
toolchainVersion=21
